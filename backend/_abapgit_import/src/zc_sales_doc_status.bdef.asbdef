projection;
define behavior for ZC_SALES_DOC_STATUS alias SalesDoc
{
  use action closeContract;
  use action completeContract;
  use action releaseContract;
  use action updatePendingRate;
  use action closeSalesOrder;
  use action closeOrderProgram;
}
